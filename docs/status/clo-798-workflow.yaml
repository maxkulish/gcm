task_id: clo-798
task_type: specification
classification:
  recommended: specification
  chosen: specification
  confidence: medium
  method: probe
  evidence:
    blast_radius:
      lean: neutral
      note: >-
        ~6 files, all existing modules: provider/http.rs, provider/identity.rs, debug.rs, main.rs, plan.rs, ui.rs.
        Additive edits, no new module.
    architecture_novelty:
      lean: dev
      note: >-
        Transport is blocking ureq (ADR-001 Decision 2). A live elapsed ticker over a blocking call needs a background
        thread plus TTY/--json gating. The only existing spinner (config/facade.rs:162,178, cliclack) is wizard-only.
    prior_art:
      lean: spec
      note: >-
        log! macro and Level::Warn already exist (debug.rs:62,15) - only a warn convenience macro is missing.
        cliclack/console already dependencies. ProviderError Display (identity.rs:43-49) is the seam for the
        actionable 400 message.
    requirement_clarity:
      lean: spec
      note: 6 scope bullets and 4 acceptance criteria, each testable; the stderr-vs---json boundary is stated explicitly.
    scope_signal:
      lean: neutral
      note: No estimate on the issue; 6 bullets reads M.
  rationale: >-
    Requirements are enumerated and testable and most infrastructure already exists; the single real design decision
    (rendering live progress over a blocking call without breaking --json or non-TTY) is narrow enough to settle
    inside the spec rather than a full design doc.
pending_human_action: null
linear:
  team: Cloud-ai
  project: GCM
  status_at_start: Backlog
  blocks: []
  blocked_by: []
  related: [CLO-797]
  branch_suggested: kmamemo/clo-798-add-progress-and-failure-visibility-to-provider-calls-so-a
  branch_actual: fix/clo-798-silent
  priority: High
  labels: [Improvement]
workflow:
  current_phase: complete
  status: complete
  created_at: '2026-09-16T20:27:39.000Z'
  updated_at: '2026-09-16T23:55:00.000Z'
phases:
  discovery:
    status: skipped
    skip_reason: Specification task
    approved: true
  design:
    status: skipped
    reason: Specification task - using /spec instead
  spec:
    status: complete
    spec_version: 2
    spec_file: docs/specs/2026-09-16-clo-798-provider-call-visibility.md
    approved: true
    review_completed: true
    review_gemini: docs/reviews/clo-798-spec-review-gemini.md
    review_ollama: null  # REVIEW_FAILED - glm-5:cloud manifest pull failed
    review_synthesis: docs/reviews/clo-798-spec-review-synthesis.md
    review_verdict: APPROVE_WITH_SUGGESTIONS  # v1 only; v2 re-review in flight
    review_applied: true
    applied_suggestions:
      - 'AC-2: pinned the unit - prompt text section bytes, not tokens, not the serialized request body'
      - 'Evaluation: added row 11, error-path ticker join (MissingKey returns before the call)'
      - 'Constraints: noted main.rs:36-39 exits via run() return, so CallProgress destructors are not skipped'
      - 'Out of scope: resolve_hunks gap documented with evidence (resolve/mod.rs:966 batch loop, :1151); follow-up issue to file'
    flagged_suggestions: []  # no reviewer item contradicted an ADR
    decisions:
      - Hand-rolled ticker thread in src/ui.rs, not cliclack (wizard widget, no non-TTY fallback)
      - '--json suppresses the ticker only; status line and retry notices still reach stderr'
      - 'JSON contract: envelope shape + v/status/mode/error.code/fallback.raw_code frozen; error.message and fallback.reason prose may change'
      - 'log_level() default moves Off -> Warn; GCM_LOG_LEVEL=off is the opt-out (without this AC-3 is a no-op)'
      - 'Timeout and context-window prose composed binary-side where the operation and its actual budget are known; ErrorKind shape frozen'
  implement:
    status: complete
    last_phase_completed: 'Codex validation gate (6 rounds)'
    sub_tasks_complete: [1, 2, 3, 4, 5, 6, 7]
    commits:
      - 231f303 feat(logging): default to warn level and coordinate stderr writes
      - d44f8f4 feat(provider): surface retries and mark context-window rejections
      - 60912a2 feat(ui): show in-flight provider progress and actionable failures
      - 5b0735c test(observability): cover the provider-call visibility criteria
      - 479d1d0 fix(ui): register the ticker width before drawing the frame
      - a421a45 fix(ui): address the Codex validation findings
      - 483edd4 fix(provider): stop output-budget rejections reading as oversized prompts
      - 36ed30f fix(provider): let explicit context-length signals outrank the output veto
      - c67e4c8 fix(provider): let window evidence outrank the output-budget veto
      - 4c21f7e fix(provider): require an overflow before reading a body as oversized
      - 4d9e0d7 test(json): freeze the value types the key sets were only implying
    tests_passing: 553
    codex_validated: true
    codex_verdict: PASS_WITH_NOTES
    codex_report: docs/reviews/clo-798-codex-validation.md
    codex_rounds: 6
    codex_round_reports:
      - docs/reviews/clo-798-codex-validation-round1.md
      - docs/reviews/clo-798-codex-validation-round2.md
      - docs/reviews/clo-798-codex-validation-round3.md
      - docs/reviews/clo-798-codex-validation-round4.md
      - docs/reviews/clo-798-codex-validation-round5.md
    codex_open_note: >-
      One LOW note accepted, not fixed: the suite asserts envelope key sets and value types rather than
      whole normalized golden envelopes. Comparing whole envelopes would freeze error.message and
      fallback.reason, which the approved spec's JSON contract decision deliberately leaves free to change.
    gemini_validation_report: null
    gemini_skip_reason: 'gemini CLI not installed on this machine'
  pr:
    status: complete
    pr_url: https://github.com/maxkulish/gcm/pull/60
    pr_number: 60
    merged_at: '2026-09-16T22:12:56.000Z'
    merge_commit: 96116ed
    ci: 'green on ubuntu-latest and macos-latest (run 35156185862)'
  complete:
    status: complete
    aggregation_files_updated: true
    merged_at: '2026-09-16T22:12:56.000Z'
    follow_ups_open:
      - 'gcm resolve provider calls are still silent (src/resolve/mod.rs:966, :1151); CallProgress is reusable there'
      - 'Timeout scaling with expected work - a Linear scope bullet the spec Must-not list excluded'
      - 'Anthropic hardcoded max_tokens: 4096 on the plan call (src/provider/anthropic.rs:161) - same exclusion'
history:
  - timestamp: '2026-09-16T20:27:39.000Z'
    action: workflow_started
    phase: init
    details: Workflow initialized for clo-798 as specification
  - timestamp: '2026-09-16T20:27:39.000Z'
    action: task_classified
    phase: init
    details: >-
      recommended=specification, chosen=specification, confidence=medium, method=probe (2 spec / 1 dev / 2 neutral).
      User accepted the recommendation.
  - timestamp: '2026-09-16T20:28:30.000Z'
    action: project_sync_start
    phase: init
    details: >-
      PROJECT.md/ROADMAP.md synced via /project:sync CLO-798 --start. WIP 0/3 before start, not blocked
      (CLO-797 is related, not blocking). Added to Active Work and to the master table (slice Obs); new
      ROADMAP section "Observability (cross-cutting)" with CLO-798 In Progress, Summary row 1/0.
      DEPENDENCIES.md unchanged - CLO-798 was in neither Current Blockers nor Unblocked & Ready.
  - timestamp: '2026-09-16T20:28:30.000Z'
    action: phase_transition
    phase: init
    details: Transitioned from init to spec
  - timestamp: '2026-09-16T20:33:32.000Z'
    action: spec_created
    phase: spec
    details: >-
      Spec written to docs/specs/2026-09-16-clo-798-provider-call-visibility.md. Scope M, 7 files, 6 sub-tasks,
      9 acceptance criteria, 11 evaluation tests. Two design decisions settled with the user before drafting:
      hand-rolled ticker over cliclack, and --json suppresses the ticker only.
  - timestamp: '2026-09-16T20:37:45.000Z'
    action: spec_review_complete
    phase: spec
    details: >-
      lok spec-review: Gemini APPROVE_WITH_SUGGESTIONS; Ollama REVIEW_FAILED (glm-5:cloud manifest pull failed, no
      second opinion). Consolidated verdict APPROVE_WITH_SUGGESTIONS, no NEEDS_REVISION findings. All three reviewer
      code claims verified against the tree before acting (main.rs:36-39, resolve/mod.rs:966 and :1151).
  - timestamp: '2026-09-16T20:37:45.000Z'
    action: spec_review_applied
    phase: spec
    details: >-
      4 suggestions auto-applied (AC-2 unit, evaluation row 11, process::exit constraint note, resolve out-of-scope
      evidence). 0 conflicts with prior decisions, so nothing was escalated. Baseline cargo test green (exit 0).
  - timestamp: '2026-09-16T20:52:59.000Z'
    action: spec_revision_requested
    phase: spec
    details: >-
      User returned a code-grounded review with 5 P1 blockers and 2 P2 gaps. All seven reproduced against the tree
      before acting: (1) output.rs:194/:180 serialize prose into error.message/fallback.reason so byte-identical JSON
      contradicts the ticket; (2) provider/facade.rs:23 shows binary and library are separate crates so a pub(crate)
      helper cannot bridge them; (3) debug.rs:39-48 defaults to Off so promoting retries to Warn emits nothing;
      (4) bad_request_detail http.rs:248-265 takes only error.message and truncates at 200 chars, discarding sibling
      code; (5) http.rs:177-181 classifies a post-header stall as Transport not Timeout, and get_once shares
      ErrorKind::Timeout with a fixed 5s budget; (6) groq.rs:62 raises MissingKey inside generate_plan so the guard is
      already live; (7) main.rs:903 gates the fallback announcement on !args.json. Additionally found while revising:
      src/debug.rs compiles into both crates (lib.rs:32 and main.rs:5) so any coordination static must live in the lib.
  - timestamp: '2026-09-16T20:52:59.000Z'
    action: spec_created
    phase: spec
    details: >-
      Spec v2 written. Sections 2-5 rewritten: 12 acceptance criteria (was 9), 7 sub-tasks (was 6), 18 evaluation
      tests (was 12), plus a four-call-site table and a findings table with per-finding evidence. Three contract
      decisions taken with the user before drafting (JSON contract, default log level, diagnostic placement).
  - timestamp: '2026-09-16T20:53:49.000Z'
    action: spec_approved
    phase: spec
    details: >-
      User approved spec v2 (message: approved). v2 AI re-review still in flight at approval time; its verdict will be
      recorded when it lands and any blocker raised before the affected sub-task. Proceeding to implement.
  - timestamp: '2026-09-16T20:53:49.000Z'
    action: phase_transition
    phase: spec
    details: Transitioned from spec to implement (spec decomposition is the plan; plan phase skipped by design)
  - timestamp: '2026-09-16T22:05:00.000Z'
    action: phase_completed
    phase: implement
    details: >-
      Sub-tasks 1-4 committed earlier (231f303, d44f8f4). Sub-tasks 5-7 landed in 60912a2:
      ui::CallProgress with an interruptible condvar wait, provider::diagnostics composing
      operation-specific context-window and both-phase timeout prose, prompt-section debug
      logging, and the transition announcement unconditional under --json.
  - timestamp: '2026-09-16T22:25:00.000Z'
    action: implementation_complete
    phase: implement
    details: >-
      tests/observability.rs adds 11 integration tests covering evaluation rows 2,3,4,6,7,
      10,11,12,13,14,15,16; row 8 (wizard model-list budget) is a library unit test because
      the wizard reads /dev/tty. 549 tests pass; cargo fmt --check, clippy -D warnings,
      scripts/check-public-surface.sh and cargo build --lib --no-default-features all clean.
  - timestamp: '2026-09-16T22:35:00.000Z'
    action: pushed_to_remote
    phase: implement
    details: fix/clo-798-silent pushed to origin
  - timestamp: '2026-09-16T23:50:00.000Z'
    action: codex_validation_complete
    phase: implement
    details: >-
      Six Codex rounds (gpt-6-astra, read-only). Rounds 1-5 returned FAIL; every verified finding was fixed:
      the ticker/log coordination race (mutex-guarded Renderer in the library), four generations of
      context-window detector false positives and false negatives, a retry-budget overflow at
      GCM_RETRY_MAX=u32::MAX, advice that suggested staging (ineffective - the prompt is built from the whole
      worktree) and raising GCM_DIFF_TOTAL_BYTES (backwards), a README overclaim about GCM_LOG_LEVEL=off,
      and incomplete JSON contract assertions. Round 6 on 4d9e0d7 returned PASS_WITH_NOTES with one LOW note
      accepted as a design decision. Gemini skipped - CLI not installed.
  - timestamp: '2026-09-16T23:55:00.000Z'
    action: phase_transition
    phase: implement
    details: Transitioned from implement to pr
  - timestamp: '2026-09-17T00:20:00.000Z'
    action: pr_created
    phase: pr
    details: >-
      Rebased onto origin/main first (branch was 7 commits behind, including the v0.7.1 release and the
      dependency upgrade in PR #58); the two doc conflicts in PROJECT.md/ROADMAP.md were resolved in
      main's favour, its versions being the newer ones. Gate re-run green after the rebase: 553 tests,
      fmt, clippy -D warnings, the no-default-features lib build and the public-surface check.
      PR #60 opened, listing the seven additive public exports as AC-9 requires.
  - timestamp: '2026-09-17T00:40:00.000Z'
    action: review_addressed
    phase: pr
    details: >-
      First CI run failed on both runners: json_contract_frozen and transition_announced_under_json let a
      run reach the commit, and gcm signs every commit (FR-4) while the runners hold no key. Both now probe
      with `git commit -S` first, the pattern tests/resolve_integration.rs:802 already uses, and the AC-11
      announcement is asserted before the guard because it is printed before the commit is attempted.
      Verified locally both ways - with a key, and with GIT_CONFIG_GLOBAL=/dev/null to force the skip.
  - timestamp: '2026-09-17T00:55:00.000Z'
    action: pr_merged
    phase: pr
    details: >-
      PR #60 squash-merged as 96116ed after CI went green on both runners. Linear CLO-798 moved to Done
      with a closing comment that names the two Scope bullets the approved spec deliberately excluded.
  - timestamp: '2026-09-17T01:00:00.000Z'
    action: workflow_complete
    phase: complete
    details: >-
      Aggregation files synced on branch docs/clo-798-finalize rather than in the main checkout: this
      session runs in the gcm--fix-clo-798-silent worktree and is not to operate in ../gcm, so the
      PROJECT.md / ROADMAP.md / DEPENDENCIES.md updates go through their own small PR.
