# Rust
/target/
**/*.rs.bk
*.pdb

# Release artifacts from `make dist` / `make dist-all`
/dist/

# env file (never commit secrets)
.env

# Editor/IDE
# .idea/
# .vscode/

# macOS
.DS_Store

# AI dev-tooling migrated from lok (.claude / .lok / .pi command + extension setup)
# Regenerated locally via `npm install` in each .pi/extensions/* dir:
node_modules/
# Per-user lok orchestrator overrides:
.lok/local/
